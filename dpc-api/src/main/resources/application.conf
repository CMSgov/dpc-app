dpc.api {
  include "server.conf"

  attributionURL = "http://localhost:3500/v1/"
  exportPath = "/tmp"

  httpClient {
    timeout: 5s
    connectionRequestTimeout: 5s
  }

  queue {
    singleServerConfig {
      address = "redis://localhost:6379"
    }
  }
}