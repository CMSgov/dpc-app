version: '3'

services:

  aggregation:
    volumes:
      - /tmp:/app/data
      - ./jacocoReport/dpc-aggregation:/jacoco-report

  api:
    volumes:
      - /tmp:/app/data
      - ./jacocoReport/dpc-api:/jacoco-report
