<% title "Team Members" %>

<div id="dashboard__title">
  <h1 class="ds-text-heading--xl ds-h3 ds-u-margin-top--1"><%= @user.implementer %></h1>
</div>

<div>
  <div class="member-list ds-u-margin-y--7">
    <h1 class="ds-text-heading--xl ds-h3 member-header">Current Users</h1>
    <ul>
      <% if @first_user.present? %>
        <li><%= @first_user.name %></li>
      <% end %>
      <% @users_accepted.each do |user| %>
        <li>
          <div>
            <%= user.name %> 
          </div>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="member-list ds-u-margin-y--7">
  <h1 class="ds-text-heading--xl ds-h3 member-header">Pending Invites</h1>
  <ul>
    <% @users_pending.each do |user| %>
      <li>
        <div>
          <%= user.name %> 
        </div>
        <div class="invited-name-container">
          (invited by <%= invited_by(user.invited_by_id) %>)
        </div>
      </li>
    <% end %>
  </ul>
</div>
</div>

<div class="ds-u-margin-y--7">
  <h1 class="ds-text-heading--xl ds-h3 member-header">Invite your team!</h1>

  <%= render partial: 'new_invite' %>
</div>