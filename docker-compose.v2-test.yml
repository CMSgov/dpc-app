version: '3'

services:

  v2-api-test:
    build:
      context: .
      dockerfile: docker/Dockerfiles/Dockerfile.v2-api-test
    env_file:
      - ./ops/config/decrypted/local.env
    command: sh -c "make test"

  v2-attribution-test:
    build:
      context: .
      dockerfile: docker/Dockerfiles/Dockerfile.v2-attribution-test
    env_file:
      - ./ops/config/decrypted/local.env
    command: sh -c "make test"
