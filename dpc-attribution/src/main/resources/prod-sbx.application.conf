dpc.attribution {
  database = {
    url = "jdbc:postgresql://db.dpc-prod-sbx.local:5432/dpc_attribution"
  }

  server {
    applicationConnectors = [{
      type = http
      port = 8080
    }]
  }
}