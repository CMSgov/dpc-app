includes:
  - "application.yml"
  - "server-ci.yml"

# server-ci.yml gets included twice, once under the namespace, once not all tests utilize
# the configuration namespace, so this ensures our logging settings apply to all tests
dpc.attribution:
    includes:
      - "server-ci.yml"
