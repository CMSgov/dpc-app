dpc.attribution {
  database = {
    driverClass = org.postgresql.Driver
    url = "jdbc:postgresql://db:5432/dpc_attribution"
    user = postgres
    password = dpc-safe
  }

  database_website = {
      driverClass = org.postgresql.Driver
      url = "jdbc:postgresql://db:5432/website"
      user = postgres
      password = dpc-safe
    }

  server {
    applicationConnectors = [{
      type = http
      port = 8080
    }]
  }
}
