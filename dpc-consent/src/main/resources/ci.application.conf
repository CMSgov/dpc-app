include "application.conf"

logging {
    level = ERROR
    loggers {
        "liquibase" = WARN
        "org.hibernate.SQL" = WARN
    }
}

dpc.consent {

  server.requestLog.appenders = []

  logging {
    level = ERROR
    loggers {
      "liquibase" = WARN
      "org.hibernate.SQL" = WARN
    }
  }

}