name: "Call Full Deploy"

on:
  push:
    paths:
      - .github/workflows/temp-caller.yml
      - .github/workflows/ecs-release.yml


jobs:
  runit:
    uses: ./.github/workflows/ecs-release.yml
    with:
      env: dev
      confirm_env: dev
      ops-ref: main
    secrets: inherit
