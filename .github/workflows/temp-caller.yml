name: 'Temp Caller - Smoke Test GHA'

on:
  push:
    paths:
      - .github/workflows/smoke-test.yml
      - .github/workflows/temp-caller.yml

jobs:
  deploy:
    name: Smoke Test Runner
    uses: ./.github/workflows/smoke-test.yml
    with:
      env: test
    secrets: inherit

