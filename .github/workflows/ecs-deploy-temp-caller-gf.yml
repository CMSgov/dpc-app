name: 'temporary caller workflow for ECS Deploy (Greenfield)'

on:
  push:

concurrency:
  group: deploy-to-${{ inputs.env }}
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read

jobs:
  call_workflow:
    uses: cmsgov/dpc-app/.github/workflows/ecs-deploy-gf.yml@ls/dpc-4715-new-relic-alert-deploy
    with:
      env: 'dev'
      confirm_env: 'dev'
      ecr_image_tag: 'latest'
      ops-ref: 'main'
    secrets: inherit
