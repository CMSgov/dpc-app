name: "DPC CI Workflow"

on:
  push:
    paths-ignore:
      - .github/workflows/opt-out-*
      - lambda/**
  workflow_dispatch: # Allow manual trigger

env:
  VAULT_PW: ${{ secrets.VAULT_PW }}
  REPORT_COVERAGE: true
  DPC_CA_CERT: ${{ secrets.DPC_CA_CERT }}
  ENV: "github-ci"

jobs:
  build-dpc-admin:
    name: "Build and Test DPC Admin Portal"
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout code"
        uses: actions/checkout@v1
      - name: "DPC Admin Portal Build"
        run: |
          echo "make ci-admin-portal"
      - name: "Move the test results" # won't upload hidden files
        run: |
          sudo mv ./dpc-admin/coverage/.resultset.json ./dpc-admin/coverage/resultset.json
      - name: Archive code coverage results
        uses: actions/upload-artifact@v3
        with:
          name: code-coverage-report-dpc-admin
          path: ./dpc-admin/coverage/resultset.json

  sonar-quality-gate-dpc-admin:
    name: Sonarqube Quality Gate for dpc-admin
    needs: build-dpc-admin
    runs-on: self-hosted
    env:
      # Workaround until https://jira.cms.gov/browse/PLT-338 is implemented.
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: "true"
    steps:
      - name: "Checkout code"
        uses: actions/checkout@v1
      - name: Download code coverage
        uses: actions/download-artifact@v3
        with:
          name: code-coverage-report-dpc-admin
      - name: Set env vars from AWS params
        uses: cmsgov/ab2d-bcda-dpc-platform/actions/aws-params-env-action@main
        env:
          AWS_REGION: ${{ vars.AWS_REGION }}
        with:
          params: |
            SONAR_HOST_URL=/sonarqube/url
            SONAR_TOKEN=/sonarqube/token
      - name: Run quality gate scan
        uses: sonarsource/sonarqube-scan-action@master
        with:
          args:
            -Dsonar.projectKey=bcda-dpc-web
            -Dsonar.scm.disabled=true
            -Dsonar.sources=./dpc-admin/app,./dpc-admin/lib
            -Dsonar.ruby.coverage.reportPaths=./dpc-admin/coverage/resultset.json
            -Dsonar.working.directory=./sonar_workspace
            -Dsonar.branch.name=${{ github.event.pull_request.head.ref }}
            -Dsonar.projectVersion=${{ github.event.pull_request.head.sha }}
            -Dsonar.qualitygate.wait=true
