name: 'Test File for calling Smoke Test on remote branch'

on:
  push:

jobs:
  temp-caller:
    uses: cmsgov/dpc-app/.github/workflows/smoke-test-gf.yml@your-branch-name@ls/dpc-4687-smoke-test-gf
    with:
      env: 'dev'

