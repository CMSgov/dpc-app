name: Go Lambda Workflow Caller

on:
  push:
    paths:
      - .github/workflows/deploy-caller.yml

jobs:
  deploy:
    name: 'Deploy Opt Out Export'
    uses: CMSgov/dpc-app/.github/workflows/opt-out-export-deploy.yml@jd/dpc-4472-dedupe-workflows
    with:
      env: test
    secrets: inherit
