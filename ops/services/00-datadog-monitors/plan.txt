module.standards.data.aws_region.secondary: Reading...
module.standards.data.aws_region.secondary: Read complete after 0s [id=us-west-2]
module.standards.data.aws_iam_policy.permissions_boundary: Reading...
module.standards.data.aws_caller_identity.this: Reading...
module.standards.data.aws_region.this: Reading...
module.standards.data.aws_ssm_parameters_by_path.ssm["datadog"]: Reading...
module.standards.data.aws_region.this: Read complete after 0s [id=us-east-1]
module.standards.data.aws_vpc.cdap_vpc: Reading...
module.standards.data.aws_caller_identity.this: Read complete after 0s [id=539247469933]
module.standards.data.aws_ssm_parameters_by_path.ssm["datadog"]: Read complete after 0s [id=/dpc/test/datadog/cicd/]
module.standards.data.aws_vpc.cdap_vpc: Read complete after 1s [id=vpc-07cac3327db239c92]
module.standards.data.aws_iam_policy.permissions_boundary: Still reading... [10s elapsed]
module.standards.data.aws_iam_policy.permissions_boundary: Read complete after 16s [id=arn:aws:iam::539247469933:policy/cms-cloud-admin/ct-ado-poweruser-permissions-boundary-policy]

OpenTofu used the selected providers to generate the following execution
plan. Resource actions are indicated with the following symbols:
  + create

OpenTofu will perform the following actions:

  # module.common_datadog_monitors.datadog_monitor.ecs_cpu_high[0] will be created
  + resource "datadog_monitor" "ecs_cpu_high" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "ECS service {{servicename.name}} CPU utilization is critically high."
      + name                = "[TEST] [dpc] ECS — CPU Utilization High"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "avg(last_10m):avg:aws.ecs.service.cpuutilization{application:dpc,environment:test} by {servicename} > 85"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "85"
          + warning  = "72"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.ecs_memory_high[0] will be created
  + resource "datadog_monitor" "ecs_memory_high" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "ECS service {{servicename.name}} memory utilization is critically high."
      + name                = "[TEST] [dpc] ECS — Memory Utilization High"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "avg(last_10m):avg:aws.ecs.service.memory_utilization{application:dpc,environment:test} by {servicename} > 85"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "85"
          + warning  = "72"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.lambda_duration[0] will be created
  + resource "datadog_monitor" "lambda_duration" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "Lambda function {{functionname.name}} p99 duration is approaching its timeout threshold."
      + name                = "[TEST] [dpc] Lambda — Duration Near Timeout"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "avg(last_5m):avg:aws.lambda.duration.p99{application:dpc,environment:test} by {functionname} > 8000"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "8000"
          + warning  = "6000"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.lambda_error_rate[0] will be created
  + resource "datadog_monitor" "lambda_error_rate" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "Lambda function {{functionname.name}} has a high error rate."
      + name                = "[TEST] [dpc] Lambda — Error Rate High"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "sum(last_5m):(sum:aws.lambda.errors{application:dpc,environment:test} by {functionname} / sum:aws.lambda.invocations{application:dpc,environment:test} by {functionname}) * 100 > 1"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "1"
          + warning  = "0"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.lambda_throttles[0] will be created
  + resource "datadog_monitor" "lambda_throttles" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "Lambda function {{functionname.name}} is being throttled."
      + name                = "[TEST] [dpc] Lambda — Throttles High"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "sum(last_5m):sum:aws.lambda.throttles{application:dpc,environment:test} by {functionname} > 10"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "10"
          + warning  = "7"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.rds_cpu_high[0] will be created
  + resource "datadog_monitor" "rds_cpu_high" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "Aurora cluster {{dbclusteridentifier.name}} CPU utilization is critically high."
      + name                = "[TEST] [dpc] Aurora RDS — CPU Utilization High"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "avg(last_10m):avg:aws.rds.cpuutilization{application:dpc,environment:test} by {dbclusteridentifier} > 85"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "85"
          + warning  = "72"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.rds_db_connections_high[0] will be created
  + resource "datadog_monitor" "rds_db_connections_high" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "Aurora instance {{dbinstanceidentifier.name}} is approaching its max connection limit."
      + name                = "[TEST] [dpc] Aurora RDS — DB Connections High"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "avg(last_10m):avg:aws.rds.database_connections{application:dpc,environment:test} by {dbinstanceidentifier} > 200"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "200"
          + warning  = "160"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.rds_deadlocks[0] will be created
  + resource "datadog_monitor" "rds_deadlocks" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "Aurora instance {{dbinstanceidentifier.name}} is experiencing deadlocks."
      + name                = "[TEST] [dpc] Aurora RDS — Deadlocks Detected"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "sum(last_10m):sum:aws.rds.deadlocks{application:dpc,environment:test} by {dbinstanceidentifier} > 1"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "1"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.rds_freeable_memory_low[0] will be created
  + resource "datadog_monitor" "rds_freeable_memory_low" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "Aurora cluster {{dbclusteridentifier.name}} is running low on freeable memory."
      + name                = "[TEST] [dpc] Aurora RDS — Freeable Memory Low"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "avg(last_10m):avg:aws.rds.freeable_memory{application:dpc,environment:test} by {dbclusteridentifier} < 256000000"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "256000000"
          + warning  = "512000000"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.rds_replica_lag_high[0] will be created
  + resource "datadog_monitor" "rds_replica_lag_high" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "Aurora replica {{dbinstanceidentifier.name}} lag is too high — read queries may return stale data."
      + name                = "[TEST] [dpc] Aurora RDS — Replica Lag High"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "avg(last_10m):avg:aws.rds.aurora_replica_lag{application:dpc,environment:test} by {dbinstanceidentifier} > 30000"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "30000"
          + warning  = "22500"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.s3_http_response_4xx[0] will be created
  + resource "datadog_monitor" "s3_http_response_4xx" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "S3 bucket {{bucketname.name}} is returning a high rate of 4xx errors."
      + name                = "[TEST] [dpc] S3 — 4xx Error Rate High"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "sum(last_5m):sum:aws.s3.4xx_errors{application:dpc,environment:test} by {bucketname} > 50"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "50"
          + warning  = "37"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.s3_http_response_5xx[0] will be created
  + resource "datadog_monitor" "s3_http_response_5xx" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "S3 bucket {{bucketname.name}} is returning 5xx errors — possible AWS-side issue."
      + name                = "[TEST] [dpc] S3 — 5xx Error Rate High"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "sum(last_5m):sum:aws.s3.5xx_errors{application:dpc,environment:test} by {bucketname} > 10"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "10"
          + warning  = "5"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.sns_failed_notifications[0] will be created
  + resource "datadog_monitor" "sns_failed_notifications" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "SNS topic {{topicname.name}} is experiencing failed notification deliveries."
      + name                = "[TEST] [dpc] SNS — Failed Notifications"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "sum(last_5m):sum:aws.sns.number_of_notifications_failed{application:dpc,environment:test} by {topicname} > 5"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "5"
          + warning  = "2"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.sqs_dlq_messages_visible[0] will be created
  + resource "datadog_monitor" "sqs_dlq_messages_visible" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "Dead letter queue {{queuename.name}} has visible messages. Investigate immediately."
      + name                = "[TEST] [dpc] SQS — DLQ Messages Visible"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "sum(last_5m):sum:aws.sqs.approximate_number_of_messages_visible{application:dpc,environment:test,queuename:*dlq*} by {queuename} > 1"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "1"
        }
    }

  # module.common_datadog_monitors.datadog_monitor.sqs_message_age[0] will be created
  + resource "datadog_monitor" "sqs_message_age" {
      + draft_status        = "published"
      + evaluation_delay    = (known after apply)
      + id                  = (known after apply)
      + include_tags        = true
      + message             = "Messages in queue {{queuename.name}} are aging beyond the acceptable threshold."
      + name                = "[TEST] [dpc] SQS — Message Age Too High"
      + new_host_delay      = 300
      + notify_no_data      = false
      + query               = "avg(last_5m):avg:aws.sqs.approximate_age_of_oldest_message{application:dpc,environment:test} by {queuename} > 300"
      + require_full_window = true
      + restricted_roles    = (known after apply)
      + tags                = [
          + "application:dpc",
          + "environment:test",
          + "managed-by:tofu",
          + "shadow-mode:true",
        ]
      + type                = "metric alert"

      + monitor_thresholds {
          + critical = "300"
          + warning  = "225"
        }
    }

Plan: 15 to add, 0 to change, 0 to destroy.

Warning: Deprecated attribute

  on .terraform/modules/standards/terraform/modules/standards/main.tf line 7, in locals:
   7:   region_dir    = regex("east|west", data.aws_region.this.name)

The attribute "name" is deprecated. Refer to the provider documentation for details.

(and 3 more similar warnings elsewhere)

─────────────────────────────────────────────────────────────────────────────

Note: You didn't use the -out option to save this plan, so OpenTofu can't
guarantee to take exactly these actions if you run "tofu apply" now.
Releasing state lock. This may take a few moments...
