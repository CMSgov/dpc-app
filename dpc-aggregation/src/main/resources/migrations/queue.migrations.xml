<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog/1.9"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog/1.9
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-1.9.xsd">

    <changeSet id="queue-v2-design" author="ronaldheft-usds">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="JOB_QUEUE_BATCH"/>
                <tableExists tableName="JOB_QUEUE_BATCH_FILE"/>
            </not>
        </preConditions>
        <createTable tableName="JOB_QUEUE_BATCH">
            <column name="batch_id" type="UUID">
                <constraints nullable="false" primaryKey="true"/>
            </column>
            <column name="job_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="organization_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="provider_id" type="VARCHAR(50)"/>
            <column name="status" type="TINYINT"/>
            <column name="priority" type="INTEGER"/>
            <column name="patients" type="TEXT"/>
            <column name="patient_index" type="INTEGER"/>
            <column name="resource_types" type="VARCHAR"/>
            <column name="aggregator_id" type="UUID" />
            <column name="update_time" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="submit_time" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="start_time" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="complete_time" type="TIMESTAMP WITH TIME ZONE"/>
        </createTable>

        <createIndex tableName="JOB_QUEUE_BATCH" indexName="job_queue_batch_job_id">
            <column name="job_id"></column>
        </createIndex>

        <createIndex tableName="JOB_QUEUE_BATCH" indexName="job_queue_batch_queue_next_item">
            <column name="status"></column>
            <column name="priority"></column>
            <column name="submit_time"></column>
        </createIndex>

        <createIndex tableName="JOB_QUEUE_BATCH" indexName="job_queue_batch_aggregator">
            <column name="status"></column>
            <column name="aggregator_id"></column>
            <column name="update_time"></column>
        </createIndex>

        <createTable tableName="JOB_QUEUE_BATCH_FILE">
            <column name="batch_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="job_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="resource_type" type="INTEGER"/>
            <column name="sequence" type="INTEGER"/>
            <column name="file_name" type="TEXT"/>
            <column name="count" type="INTEGER"/>
        </createTable>

        <addPrimaryKey tableName="JOB_QUEUE_BATCH_FILE"
                       columnNames="batch_id, resource_type, sequence"/>

        <createIndex tableName="JOB_QUEUE_BATCH_FILE" indexName="job_queue_batch_file_job_id">
            <column name="job_id"></column>
        </createIndex>

        <addForeignKeyConstraint baseTableName="JOB_QUEUE_BATCH_FILE" baseColumnNames="batch_id" constraintName="fk_job_batch_id_file"
                                 referencedTableName="JOB_QUEUE_BATCH" referencedColumnNames="batch_id"/>
    </changeSet>

    <changeSet id="add-checksum-file-length" author="nickrobison-usds">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="JOB_QUEUE_BATCH_FILE" columnName="checksum"/>
                <columnExists tableName="JOB_QUEUE_BATCH_FILE" columnName="file_length"/>
            </not>
        </preConditions>
        <addColumn tableName="JOB_QUEUE_BATCH_FILE">
            <column name="checksum" type="BYTEA"/>
            <column name="file_length" type="BIGINT"/>
        </addColumn>
    </changeSet>

    <changeSet id="since-support" author="rickhawesusds">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="JOB_QUEUE_BATCH" columnName="since"/>
                <columnExists tableName="JOB_QUEUE_BATCH" columnName="transaction_time"/>
            </not>
        </preConditions>
        <addColumn tableName="JOB_QUEUE_BATCH">
            <column name="since" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="true"/>
            </column>
            <column name="transaction_time" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="true"/>
            </column>
        </addColumn>
    </changeSet>

    <changeSet id="add-requesting-ip" author="hkxb">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="JOB_QUEUE_BATCH" columnName="requesting_ip"/>
            </not>
        </preConditions>
        <addColumn tableName="JOB_QUEUE_BATCH">
            <column name="requesting_ip" type="VARCHAR(255)">
                <constraints nullable="true"/>
            </column>
        </addColumn>
    </changeSet>

    <changeSet id="add-bulk-indicator" author="hkxb">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="JOB_QUEUE_BATCH" columnName="is_bulk"/>
            </not>
        </preConditions>
        <addColumn tableName="JOB_QUEUE_BATCH">
            <column defaultValueBoolean="false"
                    name="is_bulk"
                    type="boolean"/>
        </addColumn>
    </changeSet>

    <changeSet id="add-org-and-provider-npi" author="jonfulk">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="JOB_QUEUE_BATCH" columnName="organization_npi"/>
                <columnExists tableName="JOB_QUEUE_BATCH" columnName="provider_npi"/>
            </not>
        </preConditions>
        <addColumn tableName="JOB_QUEUE_BATCH">
            <column name="organization_npi" type="VARCHAR(50)"/>
            <column name="provider_npi" type="VARCHAR(50)"/>
        </addColumn>
    </changeSet>

    <changeSet id="add-request-url" author="jonfulk">
        <preConditions onFail="MARK_RAN">
            <not>
                <columnExists tableName="JOB_QUEUE_BATCH" columnName="request_url"/>
            </not>
        </preConditions>
        <addColumn tableName="JOB_QUEUE_BATCH">
            <column name="request_url" type="VARCHAR(255)"/>
        </addColumn>
    </changeSet>

    <!-- Create a foreign data wrapper linking to the provider table in dpc-attribution -->
    <changeSet id="create-postgres-fdw-extension" author="Mike Esposito">
        <sql>
            CREATE EXTENSION IF NOT EXISTS postgres_fdw;
        </sql>
    </changeSet>
    <changeSet id="create-foreign-attribution-server" author="Mike Esposito">
        <sql>
            CREATE SERVER IF NOT EXISTS dpc_attribution
            FOREIGN DATA WRAPPER postgres_fdw
            OPTIONS (host 'localhost', dbname 'dpc_attribution', port '5432');
        </sql>
    </changeSet>
    <changeSet id="create-fdw-user-mapping" author="Mike Esposito">
        <preConditions>
            <sqlCheck expectedResult="0">
                select count(*)
                from pg_user_mappings
                where srvname = 'dpc_attribution' and usename = '${QUEUE_DB_USER}'
            </sqlCheck>
        </preConditions>
        <sql>
            CREATE USER MAPPING IF NOT EXISTS FOR "${QUEUE_DB_USER}"
            SERVER dpc_attribution
        </sql>
    </changeSet>
    <changeSet id="import-provider-table" author="Mike Esposito">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">
                select count(*)
                from pg_class
                where relname = 'providers'
                and relkind = 'f';
            </sqlCheck>
        </preConditions>
        <sql>
            IMPORT FOREIGN SCHEMA public
            LIMIT TO (providers)
            FROM SERVER dpc_attribution
            INTO public;
        </sql>
    </changeSet>

    <changeSet id="create-quicksight-views" author="Mike Esposito">
        <!-- No preconditions are necessary since we can always recreate a new view on top of an old one -->

        <createView viewName="unique_providers_served_all_time"
                    replaceIfExists="true"
                    schemaName="public">
            select COUNT(DISTINCT(p.id)) as unique_providers_served
            from job_queue_batch j,
                 providers p
            where j.status = 2
            and (p.id::text = j.provider_id or p.provider_id = j.provider_npi)
        </createView>

        <!-- Postgres weeks are Monday to Sunday, but we want Sunday to Saturday -->
        <createView viewName="benes_requested_last_week"
                    replaceIfExists="true"
                    schemaName="public">
            select SUM(ARRAY_LENGTH(STRING_TO_ARRAY(patients, ','), 1)) as benes_requested
            from job_queue_batch
            where patients IS NOT NULL
            and submit_time AT TIME ZONE 'America/New_York' >= DATE_TRUNC('week', now() AT TIME ZONE 'America/New_York' + INTERVAL '1 day') - INTERVAL '8 day'
            and submit_time AT TIME ZONE 'America/New_York' &lt; DATE_TRUNC('week', now() AT TIME ZONE 'America/New_York' + INTERVAL '1 day') - INTERVAL '1 day'
        </createView>
        
        <createView viewName="unique_benes_served_last_week"
                    replaceIfExists="true"
                    schemaName="public">
            select COUNT(DISTINCT p.patients) as unique_benes_served
            from job_queue_batch j,
            lateral UNNEST(STRING_TO_ARRAY(j.patients, ',')) as p(patients)
            where j.patients IS NOT NULL
            and status = 2
            and j.submit_time AT TIME ZONE 'America/New_York' >= DATE_TRUNC('week', now() AT TIME ZONE 'America/New_York' + INTERVAL '1 day') - INTERVAL '8 day'
            and j.submit_time AT TIME ZONE 'America/New_York' &lt; DATE_TRUNC('week', now() AT TIME ZONE 'America/New_York' + INTERVAL '1 day') - INTERVAL '1 day'
        </createView>

        <createView viewName="bulk_calls_made_last_week"
                    replaceIfExists="true"
                    schemaName="public">
            SELECT COUNT(DISTINCT job_id) as bulk_calls
            FROM job_queue_batch
            WHERE is_bulk = true
            and submit_time AT TIME ZONE 'America/New_York' >= DATE_TRUNC('week', now() AT TIME ZONE 'America/New_York' + INTERVAL '1 day') - INTERVAL '8 day'
            and submit_time AT TIME ZONE 'America/New_York' &lt; DATE_TRUNC('week', now() AT TIME ZONE 'America/New_York' + INTERVAL '1 day') - INTERVAL '1 day'
        </createView>

        <createView viewName="patient_everything_requests_last_week"
                    replaceIfExists="true"
                    schemaName="public">
            select COUNT(*) as everything_requests
            from job_queue_batch
            where STRPOS(patients, ',') = 0
            and is_bulk = false
            and submit_time AT TIME ZONE 'America/New_York' >= DATE_TRUNC('week', now() AT TIME ZONE 'America/New_York' + INTERVAL '1 day') - INTERVAL '8 day'
            and submit_time AT TIME ZONE 'America/New_York' &lt; DATE_TRUNC('week', now() AT TIME ZONE 'America/New_York' + INTERVAL '1 day') - INTERVAL '1 day'
        </createView>

        <createView viewName="since_requests_last_week"
                    replaceIfExists="true"
                    schemaName="public">
            select COUNT(DISTINCT job_id) as since_requests
            from job_queue_batch
            where since IS NOT NULL
            and submit_time AT TIME ZONE 'America/New_York' >= DATE_TRUNC('week', now() AT TIME ZONE 'America/New_York' + INTERVAL '1 day') - INTERVAL '8 day'
            and submit_time AT TIME ZONE 'America/New_York' &lt; DATE_TRUNC('week', now() AT TIME ZONE 'America/New_York' + INTERVAL '1 day') - INTERVAL '1 day'
        </createView>
    </changeSet>
    
    <changeSet id="create-quicksight-indexes" author="Mike Esposito">
        <createIndex tableName="job_queue_batch" indexName="job_queue_batch_since_requests">
            <column name="since"></column>
            <column name="submit_time"></column>
        </createIndex>
        <createIndex tableName="job_queue_batch" indexName="job_queue_batch_everything_requests">
            <column name="is_bulk"></column>
            <column name="submit_time"></column>
        </createIndex>
    </changeSet>

</databaseChangeLog>
