dpc.aggregation {
      bbclient {
          keyStore {
              type = "JKS"
              defaultPassword = "changeit"
              location = "../bbcerts/bb.keystore"
          }

        serverBaseUrl = "http://localhost:1080/v1/fhir/"
      }

      server {
          registerDefaultExceptionMappers = false
          applicationConnectors = [{
            type = http
            port = 8080
          }]
          adminConnectors =  [{
            type = http
            port = 9999
          }]
      }

      test  {
        mockServerPort = 1080
      }

      exportPath = "/tmp"
  }
