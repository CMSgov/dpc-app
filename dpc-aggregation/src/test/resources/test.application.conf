dpc.aggregation {

  server {
    registerDefaultExceptionMappers = false
    applicationConnectors = [{
      type = http
      port = 8080
    }]
    adminConnectors = [{
      type = http
      port = 9999
    }]
  }

  exportPath = "/tmp"
}
