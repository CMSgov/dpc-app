settings:
  artifacts-dir: bzt-out/%Y-%m-%d_%H-%M-%S.%f

execution:
  - iterations: 1
    concurrency: 1
    scenario: jmeter

scenarios:
  jmeter:
    properties:
      host: ${HOST}
      admin-url: ${ADMIN_URL}
    script: src/main/resources/SmokeTest.jmx

reporting:
  - module: passfail
    criteria:
      - "fail>50%, stop as failed"
