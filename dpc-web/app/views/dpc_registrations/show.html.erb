<%= render partial: 'shared/alerts', locals: { notice: flash[:notice], alert: flash[:alert] } %>

<div>
  <p>
    <% if @dpc_registration.opt_in %>
      You've chosen to register for access to the DPC API, and your
      status is <%= @dpc_registration.status %>.
    <% else %>
      You've chosen NOT to register for access to the DPC API.
    <% end %>
  </p>
  <p>
    Registration Details:<br/>
    <%= @dpc_registration.user.name %><br/>
    <%= @dpc_registration.organization %><br/>
    <%= @dpc_registration.address_1 %><br/>
    <%= @dpc_registration.address_2 %><br/>
    <%= @dpc_registration.city %>, <%= @dpc_registration.state %> <%= @dpc_registration.zip %>
  </p>
</div>
