# Config values for testing.
# Defaults to identifying resources within the project tree
attributionURL = "http://localhost:3500/v1/"
aggregation.bbclient.keyStore.location = "../../../bbcerts/bb.keystore"
exportPath = "/tmp"