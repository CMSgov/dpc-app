# config/solid_queue.yml
production:
  dispatchers:
    - polling_interval: 10.seconds # Check for scheduled jobs less often
      batch_size: 10
  workers:
    - queues: "*"
      threads: 1                  # CRITICAL: Minimize background memory usage
      polling_interval: 5.seconds # Don't hammer the DB/CPU