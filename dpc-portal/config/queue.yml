default: &default
  dispatchers:
    - polling_interval: 1
      batch_size: 500
  workers:
    - queues: portal
      threads: 3
      processes: 1
      polling_interval: 1

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
