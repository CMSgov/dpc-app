<div>
  <div class="margin-bottom-5">&larr; <%= link_to @organization.name, organization_path(organization) %></div>
  <h1>Create a new client token</h1>
  <div>
    <h2>New token for <%= organization.name %></h2>
    <hr/>
    <%= form_tag organization_client_tokens_path(organization), method: :post do %>

    <%= render(Core::Form::TextInputComponent.new(label: 'Label',
	attribute: :label,
	hint: 'Choose a descriptive name to make your token easily identifiable to you.',
	default: params[:label],
	input_options: { maxlength: 25 })) %>
    <%= submit_tag "Create token", class: "usa-button", data: { test: "form-submit" } %>
    <% end %>
  </div>
</div>
